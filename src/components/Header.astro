---
import Brand from './Brand.astro';
import type { Vertical } from '../lib/vertical';
import { verticalLabels, verticalPaths } from '../lib/vertical';

interface Props {
  vertical?: Vertical;
}

const { vertical = 'energy' } = Astro.props;

const otherVertical: Vertical = vertical === 'energy' ? 'telecom' : 'energy';

const navLinks = [
  { href: '#servicios', label: 'Servicios' },
  { href: '#como-funciona', label: 'Cómo funciona' },
  { href: '#testimonios', label: 'Opiniones' },
  { href: '#comparativa', label: 'Comparativa' },
  ...(vertical === 'energy' ? [{ href: '#calculadora', label: 'Calculadora' }] : []),
  { href: verticalPaths[otherVertical], label: verticalLabels[otherVertical] },
];
---

<header class="site-header" data-header>
  <div class="container header-inner">
    <a href="/" class="brand" aria-label="Ahorra Sin Líos">
      <Brand />
    </a>

    <nav class="nav-desktop" aria-label="Principal">
      {navLinks.map((link) => (
        <a href={link.href} class="nav-link">{link.label}</a>
      ))}
    </nav>

    <a href="#contacto" class="cta-desktop">Contactar</a>

    <button
      type="button"
      class="menu-toggle"
      data-menu-toggle
      aria-expanded="false"
      aria-controls="mobile-menu"
      aria-label="Abrir menú"
    >
      <span class="material-symbols-outlined menu-icon" data-menu-icon>menu</span>
    </button>
  </div>

  <nav
    id="mobile-menu"
    class="mobile-menu"
    data-mobile-menu
    aria-label="Menú móvil"
    hidden
  >
    {navLinks.map((link) => (
      <a href={link.href} class="mobile-link">{link.label}</a>
    ))}
    <a href="#contacto" class="mobile-cta">Contactar</a>
  </nav>
</header>

<script>
  const header = document.querySelector<HTMLElement>('[data-header]');
  const toggle = header?.querySelector<HTMLButtonElement>('[data-menu-toggle]');
  const menu = header?.querySelector<HTMLElement>('[data-mobile-menu]');
  const icon = header?.querySelector<HTMLElement>('[data-menu-icon]');

  if (toggle && menu && icon) {
    const setOpen = (open: boolean) => {
      menu.classList.toggle('open', open);
      menu.hidden = !open;
      toggle.setAttribute('aria-expanded', String(open));
      toggle.setAttribute('aria-label', open ? 'Cerrar menú' : 'Abrir menú');
      icon.textContent = open ? 'close' : 'menu';
    };

    toggle.addEventListener('click', () => {
      setOpen(!menu.classList.contains('open'));
    });

    menu.querySelectorAll('a').forEach((link) => {
      link.addEventListener('click', () => setOpen(false));
    });

    window.addEventListener('resize', () => {
      if (window.matchMedia('(min-width: 48rem)').matches) {
        setOpen(false);
      }
    });
  }
</script>

<style>
  .site-header {
    position: fixed;
    top: 0;
    left: 0;
    right: 0;
    z-index: 50;
    background: rgba(247, 247, 244, 0.92);
    backdrop-filter: blur(0.75rem);
    border-bottom: 1px solid rgba(226, 226, 222, 0.72);
    box-shadow: var(--shadow-soft);
  }

  .header-inner {
    display: flex;
    align-items: center;
    justify-content: space-between;
    gap: 1rem;
    padding-block: 1rem;
  }

  .nav-desktop {
    display: none;
    align-items: center;
    gap: 2rem;
  }

  .nav-link {
    font-weight: 700;
    letter-spacing: 0.04em;
    color: var(--color-muted);
    transition: color 0.2s ease;
  }

  .nav-link:hover {
    color: var(--color-night);
  }

  .cta-desktop {
    display: none;
    align-items: center;
    justify-content: center;
    padding: 0.8rem 1.5rem;
    border-radius: var(--radius-xl);
    font-weight: 700;
    letter-spacing: 0.04em;
    color: #ffffff;
    background: var(--color-night);
    transition: background-color 0.2s ease, transform 0.2s ease, box-shadow 0.2s ease;
  }

  .cta-desktop:hover {
    background: var(--color-green);
    transform: translateY(-0.125rem);
    box-shadow: var(--shadow-soft);
  }

  .menu-toggle {
    display: inline-flex;
    align-items: center;
    justify-content: center;
    padding: 0.25rem;
    border: none;
    background: transparent;
    color: var(--color-night);
    cursor: pointer;
  }

  .menu-icon {
    font-size: var(--size-icon-nav);
  }

  .mobile-menu {
    display: none;
    flex-direction: column;
    gap: 0.25rem;
    padding: 0.75rem var(--gutter) 1.25rem;
    border-top: 1px solid var(--color-border);
    background: var(--color-bg);
  }

  .mobile-menu.open {
    display: flex;
  }

  .mobile-link {
    padding: 0.75rem 0;
    font-weight: 700;
    letter-spacing: 0.04em;
    color: var(--color-muted);
    transition: color 0.2s ease;
  }

  .mobile-link:hover {
    color: var(--color-night);
  }

  .mobile-cta {
    margin-top: 0.5rem;
    padding: 0.8rem 1.5rem;
    border-radius: var(--radius-xl);
    font-weight: 700;
    letter-spacing: 0.04em;
    text-align: center;
    color: #ffffff;
    background: var(--color-night);
    transition: background-color 0.2s ease;
  }

  .mobile-cta:hover {
    background: var(--color-green);
  }

  @media (min-width: 48rem) {
    .nav-desktop,
    .cta-desktop {
      display: flex;
    }

    .menu-toggle {
      display: none;
    }

    .mobile-menu {
      display: none !important;
    }
  }

  @media (max-width: 30rem) {
    .brand img {
      height: 2.5rem;
    }
  }
</style>
